# Surya OCR + training stack
# Wersje zgodne z benchmark/docker/surya/requirements.txt
surya-ocr==0.17.1
torch==2.7.1
torchvision==0.22.1
transformers==4.56.1
pillow>=10.0.0
numpy>=1.26.0

# Fine-tuning
peft>=0.14.0
datasets>=3.0.0
accelerate>=1.0.0
# evaluate  # do metryk CER/WER podczas treningu

# Opcjonalne
wandb>=0.18.0           # logowanie do W&B
# deepspeed             # na serwer z wieloma GPU
